pool:
  name: Azure Pipelines
steps:
- bash: |   
   docker run -v pwd:/t registry.videolan.org/vlc-debian-llvm-mingw:20190621125349 /bin/bash -c "cd ~/ && mkdir code && cd code && git clone https://github.com/videolan/vlc && cd vlc && ./extras/package/win32/build.sh -d"
   
  displayName: 'Build llvm-mingw'

